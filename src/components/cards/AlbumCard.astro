---
type Props = {
  title: string
  artist: string
  date: Date
  cover: string
}

const { title, artist, date, cover } = Astro.props
---

<article class="flex gap-4 bg-gray-800 p-4 shadow-lg">
  <img class="h-28" src={cover} alt={title} />
  <div class="flex flex-col justify-stretch gap-2">
    <div class="font-semibold">{title}</div>
    <div class="text-sm text-gray-400">{artist} • {date.getFullYear()}</div>
  </div>
</article>
